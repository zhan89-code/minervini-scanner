# Copy to .env.local for local overrides, or set in your deploy platform
# (e.g. Vercel project settings) as an environment variable.

# URL of the deployed backend. Omit for local dev -- defaults to
# http://localhost:8000.
VITE_API_BASE=https://your-backend-host.example.com
